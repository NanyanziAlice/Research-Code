#!/bin/sh 
# Copyright 2016 The MathWorks, Inc.
MYPATH=$(dirname "$0")
LINK_CLIENT="$MYPATH"/diffLinkClient

DIFF_MAIN=com.mathworks.toolbox.difflink.client.DiffClientApplication

"$LINK_CLIENT" "$DIFF_MAIN" "$1" "$2"
